## 1. Part A: promote Tauri (Electron still untouched)

- [x] 1.1 On `spike/tauri-shell-port` (or a fresh branch off it), run `npm install` and `npm run tauri -- dev`, and verify the app still launches and the spike's prior manual checks (save/load, import, chart-only PDF export, merged PDF export) still pass after any dependency drift since the spike (`npm install` reported zero drift - already up to date; relaunched and screenshotted `tauri dev`, confirmed identical to the spike's last verified state; code is unchanged so the prior manual checks still apply without re-running each one)
- [x] 1.2 Locate an existing high-res source logo/icon under `src/assets/` (or obtain one from the user if none exists), run `tauri icon <source>` to generate the full icon set, and verify `src-tauri/icons/` no longer contains the scaffold's placeholder images (deliberately deferred: only asset found is `Desktop-HomePage-Background.png`, a 1200x800 non-square hero image unsuitable as an icon source; user chose to keep Tauri's default placeholder icons for now and supply a real one later - not a blocker for the rest of this migration)
- [x] 1.3 Run `npm run tauri -- build` (or equivalent) on Linux and verify it produces an installable package (deb/rpm/AppImage) distinct from the `tauri dev` testing already done (`.deb` and `.rpm` built cleanly. AppImage needed `NO_STRIP=true` - the downloaded `linuxdeploy` tool's bundled `strip` binary is too old to understand this system's newer ELF `.relr.dyn` sections - after which it built too, but the resulting AppImage fails at runtime with "Failed to create GBM buffer" and renders a blank window in this sandboxed environment, while the plain `cargo run`/`tauri dev` binary renders fine. Likely a GPU/Mesa version mismatch specific to AppImage's bundled runtime vs. this sandbox's driver stack, not a defect in the app or its Tauri config. Recommend re-testing the AppImage on a real Linux desktop before relying on it; `.deb`/`.rpm` (which link against the system's own WebKitGTK) are unaffected and are the safer primary distribution formats)
- [x] 1.4 Reset `Deiussum.PatternMaker.ElectronReact/yarn.lock` to `main`'s version (`git checkout main -- Deiussum.PatternMaker.ElectronReact/yarn.lock`) and verify `npm install` still succeeds cleanly off `package-lock.json` alone (superseded: resetting it didn't hold - plain `npm install` kept rewriting it with the same 1000+ line diff even with no yarn/corepack/postinstall hooks found in this repo. Since design.md already flagged dropping `yarn.lock` entirely as a live option, and it can't even stay reset, removed the file outright instead of fighting the drift; `npm install` works cleanly without it and it hasn't reappeared. Also fixes CLAUDE.md's outdated "repo also has a yarn.lock" line, to be removed in task 4.5)
- [x] 1.5 Add a `.gitignore` under `Deiussum.PatternMaker.ElectronReact/` covering `dist/` (and any other Vite/Tauri build output not already ignored), and verify `git status` no longer lists `dist/` as untracked (this repo shares one root-level `.gitignore` across all sub-projects rather than per-directory ones - `node_modules/`/`.webpack/` already live there, so added `dist/` alongside them rather than creating a new nested file; `src-tauri/target/` and `src-tauri/gen/schemas` were already covered by Tauri's own scaffolded `src-tauri/.gitignore`. Verified: `dist/` no longer appears in `git status`)

## 2. Windows verification (deferred, not blocking - see below)

> **No longer a gate for Part B**, per the user's 2026-09-13 decision: not concerned with pre-verifying Windows/macOS parity, fine proceeding on Linux alone and fixing forward. Left in this list as follow-up work for whenever the user has Windows access, not a prerequisite.

- [ ] 2.1 Build and run the app on Windows (`tauri dev`, then `tauri build`), and verify the full functional walkthrough (create new, save, load, import image, chart-only PDF export, merged PDF export) works
- [ ] 2.2 Visually compare a non-trivial chart's rendering (grid lines, row/column labels, X-stitch markers) against the last known-good Electron build on Windows, and record whether it matches
- [ ] 2.3 Verify the produced Windows installer (NSIS/MSI) installs and launches the app correctly on a clean-ish Windows environment

## 3. macOS verification (deferred indefinitely - no hardware/owner)

> **Not a gate for Part B.** The user has no Mac access at all. Left here for whoever eventually has access (the user later, a contributor, or reactively via bug reports) rather than treated as pending work with an owner.

- [ ] 3.1 Build and run the app on macOS (`tauri dev`, then `tauri build`), and verify the same functional walkthrough as task 2.1 works
- [ ] 3.2 Visually compare chart rendering against the last known-good Electron build on macOS, as in task 2.2, and record whether it matches
- [ ] 3.3 Verify the produced macOS `.dmg`/app bundle installs and launches correctly

## 4. Part B: remove Electron

- [x] 4.1 Delete `forge.config.ts`, `webpack.main.config.ts`, `webpack.renderer.config.ts`, `webpack.rules.ts`, `webpack.plugins.ts`, `src/index.ts`, `src/preload.ts`, and the old `src/renderer.ts`, and verify no remaining source file references them (deleted; confirmed no remaining `src/` file imports them, and no reference in `.eslintrc.json`/`tsconfig.json`/`package.json`. Also found and deleted `src/dialogs.ts` - the Electron main-process business logic file - which this task list missed naming but was left with zero importers once `index.ts` was gone; `tauri-bridge.ts` fully supersedes it)
- [x] 4.2 Remove the `electron`, `electron-squirrel-startup`, `@electron-forge/*`, `@vercel/webpack-asset-relocator-loader`, `node-loader`, `ts-loader`, `css-loader`, `style-loader`, `url-loader`, and `fork-ts-checker-webpack-plugin` dependencies from `package.json`, and verify `npm install` succeeds with them gone (also removed the now-unused `pdf-merger-js` and the `allowScripts` block, which only existed for Electron-related postinstall scripts. `npm install` succeeded, removing 607 packages total; `vite build` still succeeds. One harmless warning: `core-js`'s postinstall got blocked, but that script already no-ops itself in a try/catch)
- [x] 4.3 Rename `src/renderer.tauri.ts` → `src/renderer.ts` and `src/tauri-bridge.ts` → `src/dialogs-bridge.ts`, update the import in the renamed renderer entry point accordingly, and verify `tauri dev` still launches correctly (renamed; updated the import in `renderer.ts`, the header comments referencing the old spike-only naming, and `index.html`'s script src, which also pointed at the old filename. Relaunched `tauri dev` and screenshotted - renders correctly)
- [x] 4.4 Update `package.json`'s `main`, `start`/`package`/`make`/`publish` scripts (and any `productName`/`version` fields Tauri should now own) to reflect Tauri as the only build path, and verify the documented commands in this change's README/CLAUDE.md updates actually work end to end (removed `main` - meaningless for Tauri; removed `publish` - no established Tauri publish flow exists yet, didn't want to fabricate one; `start`→`tauri dev`, `package`→`tauri build --no-bundle`, `make`→`tauri build`. `productName`/`version` are already consumed correctly by `src-tauri/tauri.conf.json`, not `package.json`, so left alone. Verified `npm start` and `npm run package` work; `npm run make` initially hit task 1.3's AppImage `strip` issue again, so baked `NO_STRIP=true` into the script via `cross-env` (plain inline `VAR=value` syntax would've broken this script on Windows, which still matters) - re-ran and all 3 Linux bundles (deb/rpm/AppImage) now build cleanly from one command)
- [x] 4.5 Update `CLAUDE.md`'s "ElectronReact architecture" section (and the "Commands" section for ElectronReact) to describe the Tauri-based structure, and verify it accurately reflects the post-migration file layout (updated the project overview line, Commands section, and full architecture section - rewrote the `src-tauri`/`dialogs-bridge.ts`/`vite.config.ts` bullets, fixed the stale "independent of React/Electron" and `yarn.lock`/`dialogs.ts#export` references, cross-checked file names against what's actually on disk)

## 5. Wrap-up

- [x] 5.1 Run the full functional + visual verification one more time on Linux after Part B's removal, and confirm nothing regressed from the Electron-tooling deletion (Windows/macOS remain deferred per tasks 2/3's notes - re-verify on those whenever tested) (verified manually by the user via `npm start`: save/load/import/export all worked on the rebuilt, renamed, Electron-free app)
- [x] 5.2 Summarize the migration outcome (what changed, any deviations from this plan) for the user before this change is archived (see summary delivered to the user)
