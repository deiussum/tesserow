## 1. Verify specs against current source

- [x] 1.1 Re-read `src/Mosaic.ts` against `specs/mosaic-domain-model/spec.md` and confirm every requirement (numbering, alternating color/edge locking, toggle rules, written-pattern format, save/load shape) matches the code as written, with no invented behavior (found and fixed 3 inaccuracies: column 1 is rightmost not leftmost, the written pattern lists rows starting at row 1/bottom not the top, and its per-row stitch order reads from column 1 toward the width-numbered column)
- [x] 1.2 Re-read `src/NewMosaicForm.tsx` against `specs/new-mosaic/spec.md` and confirm the required-fields and extra-rows behavior matches (confirmed accurate; extra rows are added to both top and bottom, doubling the count added)
- [x] 1.3 Re-read `src/dialogs-bridge.ts` (`importImage`/`resize`/`getImageData`) and `src/ImagePreviewDialog.tsx` against `specs/image-import/spec.md` and confirm the downscale threshold, threshold-preview, resize-linking, and cell-conversion behavior matches (confirmed accurate; traced the pixel-to-cell loop's row/col-0 skip and confirmed it only ever lands on cells already locked by construction rules when there are no extra rows — a subtler interaction where extra rows shift that skip onto an interior row exists but is too implementation-specific/edge-case to state as a spec-level requirement, so it was deliberately left undocumented)
- [x] 1.4 Re-read `src/MosaicEditor.tsx` and `src/WrittenPatternDialog.tsx` against `specs/mosaic-editor/spec.md` and confirm the zoom, hover/click, written-pattern, and toolbar-action behavior matches (confirmed accurate; noted the on-screen dialog and the PDF export use different line-wrap widths for the written pattern, which the spec already abstracts away by not hardcoding a width)
- [x] 1.5 Re-read `src/app.tsx` (`loadFile`) and `src/dialogs-bridge.ts` (`open`/`save`) against `specs/file-persistence/spec.md` and confirm the load/save/cancel behavior matches, including that a cancelled save still reports completion (confirmed accurate — `saveClicked` never inspects the result of `window.dialogs.save`)
- [x] 1.6 Re-read `src/ExportDialog.tsx` and `src/dialogs-bridge.ts` (`drawChartPage`/`renderPdfBytes`/`exportPdf`) against `specs/pdf-export/spec.md` and confirm the option defaults, validation, page layout, numbering, and cover-PDF merge behavior matches (found and documented one real quirk: `drawChartPage` unconditionally calls `doc.addPage()` after every chart page, including the last, so a chart-only export — written pattern unchecked — always has one extra blank, numbered page appended)

## 2. Validate and land the baseline

- [x] 2.1 Run `openspec validate baseline-current-specs --type change --strict` and verify it reports the change as valid (confirmed: "Change 'baseline-current-specs' is valid")
- [x] 2.2 Sync the delta specs into `openspec/specs/` (via `/opsx:sync` or by archiving this change) and verify `openspec/specs/` now contains all six capability directories with no `TBD` Purpose placeholders (confirmed: all 6 created with real Purpose sections, `openspec validate --specs --strict` reports 6/6 passed)
